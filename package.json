{
  "name": "rfh-hospital-management",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "kill-port 3000 5000",
    "dev": "concurrently \"npm run backend\" \"npm --prefix frontend run dev\"",
    "backend": "backend-java\\mvnw.cmd -f backend-java/pom.xml spring-boot:run",
    "install:all": "npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1"
  }
}
